

function multiply(num1,num2){ 
    return num1 * num2;
} 
multiply(31,2);

function random(num) {
    return Math.floor(Math.random() * 10)
}
random(1)


function mod(num3, num4) {
    return num3 % num4
}
mod(64,10)

function max(num5, num6, num7, num8, num9) {
    return Math.max(num5, num6, num7, num8, num9);
}
max(1, 2, 3, 4, 20)
